@model ProyectoBasesDatos.Models.Medicamento

@{
    ViewData["Title"] = "Editar Medicamento";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0">
        <div class="card-header bg-primary text-white py-4">
            <h1 class="h4 mb-0">
                <i class="fas fa-pills me-2"></i>Editar Medicamento
            </h1>
        </div>
        <div class="card-body p-5">
            <form asp-action="Edit" class="needs-validation" novalidate>
                <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                <input type="hidden" asp-for="Id" />

                <input type="hidden" asp-for="IdHospitalMedicamento" />

                <div class="mb-4">
                    <label asp-for="Nombre" class="form-label fw-bold">Nombre del Medicamento</label>
                    <input asp-for="Nombre" class="form-control form-control-lg" placeholder="Ingresa el nombre del medicamento" required />
                    <span asp-validation-for="Nombre" class="text-danger small"></span>
                    <div class="invalid-feedback">Por favor, ingresa el nombre del medicamento.</div>
                </div>

                <div class="mb-4">
                    <label asp-for="Descripcion" class="form-label fw-bold">Descripción</label>
                    <textarea asp-for="Descripcion" class="form-control" rows="4" placeholder="Ingresa una descripción del medicamento"></textarea>
                    <span asp-validation-for="Descripcion" class="text-danger small"></span>
                </div>

                <div class="mb-4">
                    <label for="Cantidad" class="form-label fw-bold">Cantidad</label>
                    <input name="Cantidad" class="form-control form-control-lg" type="number" min="0" value="@ViewBag.Cantidad" placeholder="Ingresa la cantidad disponible" required />
                    <div class="invalid-feedback">Por favor, ingresa una cantidad válida.</div>
                </div>

                <div class="mb-4">
                    <label for="Precio" class="form-label fw-bold">Precio</label>
                    <div class="input-group">
                        <span class="input-group-text">₡</span>
                        <input name="Precio" class="form-control form-control-lg" type="number" step="0.01" min="0" value="@ViewBag.Precio" placeholder="Ingresa el precio del medicamento" required />
                    </div>
                    <div class="invalid-feedback">Por favor, ingresa un precio válido.</div>
                </div>

                <div class="d-grid gap-3 d-md-flex justify-content-md-end mt-5">
                    <a asp-action="Index" class="btn btn-outline-secondary btn-lg d-flex align-items-center">
                        <i class="fas fa-arrow-left me-2"></i> Volver a la Lista
                    </a>
                    <button type="submit" class="btn btn-primary btn-lg d-flex align-items-center">
                        <i class="fas fa-save me-2"></i> Guardar Cambios
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <!-- Script para validación del formulario -->
    <script>
        (function () {
            'use strict';

            // Selecciona todos los formularios con la clase 'needs-validation'
            var forms = document.querySelectorAll('.needs-validation');

            // Itera sobre los formularios y previene el envío si no son válidos
            Array.prototype.slice.call(forms).forEach(function (form) {
                form.addEventListener('submit', function (event) {
                    if (!form.checkValidity()) {
                        event.preventDefault();
                        event.stopPropagation();
                    }

                    form.classList.add('was-validated');
                }, false);
            });
        })();
    </script>
}

<!-- Estilos adicionales -->
<style>
    .card {
        border: none;
        border-radius: 15px;
        overflow: hidden;
    }

    .card-header {
        border-radius: 15px 15px 0 0;
    }

    .card-footer {
        border-top: 1px solid rgba(0, 0, 0, 0.1);
    }

    .btn {
        min-width: 140px;
        padding: 0.5rem 1rem;
        font-size: 1rem;
        border-radius: 8px;
    }

    .btn-outline-secondary {
        border-color: #6c757d;
        color: #6c757d;
    }

        .btn-outline-secondary:hover {
            background-color: #6c757d;
            color: #fff;
        }

    .btn-primary {
        background-color: #0d6efd;
        border-color: #0d6efd;
    }

        .btn-primary:hover {
            background-color: #0b5ed7;
            border-color: #0b5ed7;
        }

    .text-primary {
        color: #0d6efd !important;
    }

    .card-title i {
        font-size: 1.2em;
    }

    hr {
        border-top: 2px solid rgba(0, 0, 0, 0.1);
    }
</style>

<!-- Font Awesome para íconos -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />